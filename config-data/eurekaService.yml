server:
  port: 8761

  spring:
    application:
      name: eurekaService
    cloud:
      config:
        import-check:
          enabled: false

  eureka:
    client:
      register-with-eureka: false
      fetch-registry: false
      service-url:
        defaultZone: http://localhost:8761/eureka/